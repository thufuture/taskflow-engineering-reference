# TaskFlow local database configuration.
# The application reads process environment variables and does not automatically
# load this file. Export the value in your shell or use trusted local tooling.
DATABASE_URL=sqlite:///./todo.db
